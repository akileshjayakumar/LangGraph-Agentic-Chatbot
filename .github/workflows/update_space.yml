name: Run Python script

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install gradio huggingface_hub

    # Debug Step: Check if HF_TOKEN is set and print token length
    - name: Verify HF_TOKEN
      run: |
        echo "Token length: ${#HF_TOKEN}"
      env:
        HF_TOKEN: ${{ secrets.hf_token }}

    # Use huggingface-cli login to avoid Python interactive issues
    - name: Log in to Hugging Face
      env:
        HF_TOKEN: ${{ secrets.hf_token }}
      run: echo "${HF_TOKEN}" | huggingface-cli login

    - name: Deploy to Spaces
      env:
        HF_TOKEN: ${{ secrets.hf_token }}
      run: yes "" | gradio deploy
